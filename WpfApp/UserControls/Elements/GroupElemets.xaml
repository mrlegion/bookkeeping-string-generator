<UserControl x:Class="WpfApp.UserControls.Elements.GroupElemets"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp.UserControls.Elements"
             mc:Ignorable="d"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
             d:DesignHeight="450" d:DesignWidth="800">
    <Expander BorderThickness="0"
              x:Name="GropExpander"
              Background="{StaticResource PrimaryHueMidBrush}"
              Padding="0">
        <Expander.Header>
            <TextBlock x:Name="Header" Margin="0 10" Style="{StaticResource MaterialDesignSubheadingTextBlock}" FontWeight="SemiBold" />
        </Expander.Header>
        <StackPanel Orientation="Vertical" x:Name="Root">
        </StackPanel>
    </Expander>
</UserControl>
